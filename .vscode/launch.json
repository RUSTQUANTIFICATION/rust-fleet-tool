{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run FastAPI",
            "type": "debugpy",
            "request": "launch",
            "module": "uvicorn",
            "args": [
                "main:APP",
                "--reload",
                "--port",
                "8000"
            ],
            "cwd": "${workspaceFolder}/api"
        }
    ]
}